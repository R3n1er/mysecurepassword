const fs = require("fs"); const content = fs.readFileSync("tests/components/GoogleWorkspace-Strategy.test.tsx", "utf8"); const newContent = content.replace("expect(screen.getByText(/évite les caractères non compatibles/i)).toBeInTheDocument();", "expect(screen.getByText(/évite les caractères non compatibles/i)).toBeInTheDocument(); expect(screen.getByText(/qui peuvent causer des erreurs/i)).toBeInTheDocument();"); fs.writeFileSync("tests/components/GoogleWorkspace-Strategy.test.tsx", newContent); console.log("Test amélioré");
